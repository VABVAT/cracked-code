// tells which typescript to compile, src/electron has its own compiling rules
// electron/tsconfing is for 
{
  "files": [],
  "include": ["src"],
  "exclude": ["src/electron"],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" } 
  ]
}
